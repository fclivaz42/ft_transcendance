services:
  usermanager:
    build: ./
    container_name: sarif_usermanager
    ports:
      - "3000:3000"
    environment:
      - TZ=Europe/Zurich
      - API_KEY
    env_file:
      - .env
    restart: on-failure
    networks:
      - sarifntw